fastapi==0.109.0
uvicorn[standard]==0.27.0
transformers==4.37.0
torch==2.1.2
numpy==1.26.3
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0
redis==5.0.1
google-cloud-pubsub==2.19.0

# Scrapers — MOVED to crawl-agent service
# aiohttp, telethon, beautifulsoup4, lxml, fake-useragent are no longer needed here


# Vietnamese NLP word segmentation (optional — graceful fallback)
py_vncorenlp==0.1.4

# OpenTelemetry — distributed tracing to Jaeger via OTLP HTTP
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-exporter-otlp-proto-http==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0

# ONNX Runtime — optimized inference for PhoBERT (optional)
onnxruntime==1.17.0
onnx==1.15.0

# gRPC — high-performance inter-service communication
grpcio==1.60.0
grpcio-tools==1.60.0
grpcio-reflection==1.60.0

# Fine-tuning pipeline dependencies (optional — install for training)
pandas>=2.1.0
scikit-learn>=1.3.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

